#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../image-to-slices/bin/image-to-slices" "$@"
  ret=$?
else 
  node  "$basedir/../image-to-slices/bin/image-to-slices" "$@"
  ret=$?
fi
exit $ret
